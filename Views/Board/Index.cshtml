@model IEnumerable<MvcStart.Models.Board>

@{
    ViewData["Title"] = "Index";
}

<h1>게시판</h1>

<p>
    <a asp-action="Create">글쓰기</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th width="100">
                조회수
            </th>
            <th width="100">
                좋아요
            </th>
            <th width="400">
                제목
            </th>
            <th>
                작성자
            </th>
            <th>
                작성일자
            </th>
            @*            <th>
            @Html.DisplayNameFor(model => model.letter)
        </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td width="100">
                @Html.DisplayFor(modelItem => item.click)
            </td>
            <td width="100">
                @Html.DisplayFor(modelItem => item.like_)
            </td>
            <td width="400">
                <a asp-action="Details" asp-route-id="@item.ID">@Html.DisplayFor(modelItem => item.title)</a>@*@Html.DisplayFor(modelItem => item.title)*@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.post_time)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.letter)
            </td>*@
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
